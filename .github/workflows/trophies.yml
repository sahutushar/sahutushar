name: Update Trophies

on:
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Trophies are served via live API
        run: echo "Trophies use github-trophies.vercel.app live API in README, no action needed."
